{% extends "layout.html.twig" %}
{% block contenu %}
    <body id="bg">
    <!-- start: Content -->

    <div class="row-fluid">

        <div id="content">
            <div id="Events" class="col-md-12">
                <div class="col-md-4">
                    <a href="{{ path('Objet.pShow') }}" id="Events"><h4 class="text-blanc">Vos Annonces</h4></a>
                </div>
                <div class="col-md-4">
                    <a href="{{ path('Objet.add') }}" id="Events"><h4 class="text-blanc">Ajouter une annonce</h4></a>
                </div>
                <div class="col-md-4">
                    <form method="post" action="{{ path('Objet.search')  }}" >
                        <input name="text" type="text" value="Chercher une Annonce">
                        <input type="submit" value="Valider" />
                    </form>
                </div>


            </div>
            {#<div id="sidenavbar" class="col-md-3">#}
            {#<div class="nav-collapse sidebar-nav">#}
            {#<ul class="nav nav-tabs nav-stacked main-menu">#}
            {#<li><a href="{{ path('produit.show')}}"><span class="hidden-tablet"> Acceuil</span></a></li>#}
            {#<li><a href="index.html.twig">Message</a></li>#}
            {#<li><a href="{{ path('Objet.show')}}">Objets</a></li>#}
            {#<li><a href="index.html.twig">Prof</a></li>#}

            {#</ul>#}
            {#</div>#}

            {#</div>#}
            {% for objet in data if data is not empty %}
                <div class="col-md-4 col-lg-4 col-md-4">
                    <div class="thumbnail">
                        <div class="caption">
                            <div class="col-md-5">
                                <h4>{{ objet.nom_objet }}</h4>
                            </div>
                            <div class="col-md-2">
                                <h6>{{ objet.prix_objet }}€</h6>
                            </div>
                            <div class="col-md-5">
                                <form method="post" action="{{ path('Objet.getUser') }}" >
                                    <input type="hidden" name="user" value="{{ objet.id_user }}">
                                    <input type="submit" value="Voir la personne" />
                                </form>
                            </div>
                            <div class="col-md-8">
                                <h6>{{ objet.description_objet }} ( sur {{ objet.lieu_objet }} )</h6>
                            </div>

                            <div class="col-md-4">

                                <form action="{{ path('Objet.edit', {id: objet.id_objet}) }}" >
                                    <input type="submit" value="Modifier" /></form>

                            <form action="{{ path('Objet.delete', {id: objet.id_objet}) }}" >
                                    <input type="submit" value="Supprimer" /></form>

                            </div>

                        </div>
                    </div>
                </div>
            {% endfor %}




        </div>
    </div>


    </body>

{% endblock %}

